apiVersion: v1
kind: Service
metadata:
  name: {{ template "antd-boilerplate.fullname" . }}
  labels:
    app: {{ template "antd-boilerplate.name" . }}
    chart: {{ template "antd-boilerplate.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "antd-boilerplate.name" . }}
    release: {{ .Release.Name }}
